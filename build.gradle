apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'application'

repositories {
    maven { url 'https://repo.gradle.org/gradle/repo' }
}

dependencies {
    compile 'org.gradle:gradle-tooling-api:2.13'
}

mainClassName = 'org.gradle.launcher.Main'

